# prod profile - 生产配置
# 生产关闭 SpringDoc 转发（不暴露 API 文档；内网需要时置 SPRINGDOC_ENABLED=true）
springdoc:
  api-docs:
    enabled: ${SPRINGDOC_ENABLED:false}
  swagger-ui:
    enabled: ${SPRINGDOC_ENABLED:false}
